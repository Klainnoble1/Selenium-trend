# n8n webhook URL (must accept POST for full payloads with article content)
N8N_WEBHOOK_URL=https://klain-n8n.hf.space/webhook-test/5c7459c0-cb2f-4839-94ea-14ed1da0bce9
# Run browser in headless mode (true/false)
HEADLESS=true

# Optional: N8N_WEBHOOK_METHOD=POST (default) or GET. Use POST when payload includes article content.
# Optional: limit countries (comma-separated geo codes). Leave empty for all (US, GB, CA, DE, CH).
# COUNTRIES=US,GB,CA,DE,CH
